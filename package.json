{
  "name": "tuple-database",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "npm run test:clean && mocha -r ts-node/register './src/**/*.test.ts' --verbose",
    "test:clean": "rm -rf build/test",
    "test:watch": "npm test -- --watch --watch-extensions ts",
    "publish:prepare": "npm run build && mv build out && cp package.json out && cp .npmignore out"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^5.4.1",
    "@types/fs-extra": "^9.0.5",
    "@types/lodash": "^4.14.165",
    "@types/mocha": "whitecolor/mocha-types",
    "@types/node": "^14.14.14",
    "better-sqlite3": "^7.1.2",
    "mocha": "^8.2.1",
    "prettier": "^2.2.1",
    "react": "^17.0.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "elen": "^1.0.10",
    "fractional-indexing": "^1.2.1",
    "fs-extra": "^9.0.1",
    "lodash": "^4.17.20",
    "md5": "^2.3.0",
    "ts-node": "^9.1.1",
    "uuid": "^8.3.2"
  }
}
